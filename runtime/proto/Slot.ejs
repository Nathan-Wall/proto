function CreateSlot(proto, value) {
	var obj;
	if (proto === undefined)
		proto = SlotProto;
	if (!IsObject(proto))
		throw new TypeError('Object expected');
	obj = CreateObject(proto);
	SlotInit(obj, value);
	return obj;
}

function SlotInit(obj, value) {
	if (value === undefined)
		value = null;
	else if (value !== null && !(IsObject(value) && 'RangeFrom' in value)) {
		value = ToNumber(value);
		// This check is because `(-0) | 0` is `+0`
		if (value != 0)
			value = value | 0;
	}
	obj.Slot = value;
}