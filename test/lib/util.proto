import { it as _it } from '@env';

fn it(desc, callback) :{
	_it(desc, async(done) :{
		try :{ await callback(); }
		catch (x) :{
			done(x);
			return;
		}
		done();
	});
}

export { it };
export { describe, assert } from '@env';