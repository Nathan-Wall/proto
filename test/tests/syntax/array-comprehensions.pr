import { describe, it, assert } from '../../lib/util.pr';

describe('Array Comprehensions', fn :{

	it('Works', fn :{

		var array = [ for x of 0..10: 2 * x ];

		assert.equal(array.length, 10);

	});

});